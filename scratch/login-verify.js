const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); try { await page.goto('http://localhost:3001/portal.html'); console.log('Page title:', await page.title()); await page.fill('#login-email', 'admin@wajina.edu.ng'); await page.fill('#login-password', 'password123'); console.log('Login form interactive'); } catch (e) { console.error('Login verification failed:', e.message); process.exit(1); } finally { await browser.close(); } })();